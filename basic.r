#assigement operator

h="Direct"
h
h <-"Left assignemt"
h
"Right Assingment" ->h
h
#data Type

a=5
class(a)
b="HITESH"
b=TRUE
b=22.5L
class(b)

c="HITESH"
class(c)

d=17L
class(d)

e=1+2i
class(e)

#cat: Concatenate and Print
#cat(… , file = "", sep = " ", fill = FALSE, labels = NULL, append = FALSE)
cat("Data Type of Vaiable a is", class(a), " testing")

cat(DATA1 , Data)




#print: Print method for objects of class PSTf and PSTr (Display a probabilistic suffix tree)
#print(x, max.level = NULL, digits = 1, give.attr = FALSE, nest.lev = 0, indent.str = "", stem = "--")
print(c("Testing", "code"))

#paste: Concatenate strings
#paste(..., sep=" ", collapse=NULL)
#paste concatenates vectors of strings or vector-like objects containing strings.
paste(c("TESTING" , "CODE") , c("TESTING 1" , "CODE 2") , sep = ", ")
paste("1st", "2nd", "3rd", sep = ", ")
